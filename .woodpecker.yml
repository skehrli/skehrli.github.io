steps:
  build-web:
    image: silex/emacs:ci
    when:
      branch: [ master ]
    secrets: [ cloudflare_account_id, cloudflare_api_token, repo_token ]
    commands: |
      apt install nodejs
      ./publish.sh
